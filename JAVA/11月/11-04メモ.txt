
java.lang(import文書く必要がない)
基本的なクラス
java.io
出力入力
Java.util
便利パッケージ
java.net
ネットワーク
java.sql
DBアクセス


import文
import java.util.Arrays;
import java.io.BufferedReader;
import java.io.File;
import java.io.FileReader;
import java.util.*; //util全部取り出す

Array

配列並び替え-sort

String

文字列の長さ-length()
指定位置文字検索-charAt() //0から
大文字変換-toUpperCase()
文字列区切り-String.join(",", "a" , "b", "c")

文字列可変-StringBuilder
StringBuilder MyStringBuilder 
= new StringBuilder("Hello World!");


File
ファイルルート指定
FileReader
読み取りのファイル指定
BufferedReader
読み取る
readeLine
改行まで読み取る


try{
        .....
}
 catch(Exception e)
{ 
      e.printStackTrace() ; //エラー印刷
} 
flush()//数据缓存
throws IOException 
//エラー宣言出す


Vector

List<String> list = new Vector<>();
list.add("1");//追加
list.remove("2");//削除

ArrayList

List<String> list = new ArrayList<>();
list.add("1");//追加
list.remove("2");//削除

线程不安全使用ArrayList，线程安全使用Vector。

HashMap

 HashMap<String, String> map = new HashMap<>();

 map.put("red", "赤");//キーとバリュー設定

System.out.println(map.get("red"));//当てはまるのバリュー取り出す
遍历
for (Map.Entry<String, String> entry : map.entrySet()) {
      System.out.println("key= " + entry.getKey() + " and value= " + entry.getValue());
}

生成a~b之间的随机数，只需要使用 r.nextInt(b-a+1)+a即可。